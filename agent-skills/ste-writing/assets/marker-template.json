{
  "_comment": [
    "Copy this to the root of a repository as .ste-writing.json to switch STE on",
    "for that repository. The file does two jobs: it is the opt-in switch the",
    "Stop hook looks for, and it holds this project's settings.",
    "",
    "Commit it. The glossary is a project asset, and someone who clones the",
    "repository should get the opt-in and the technical nouns together.",
    "",
    "Delete the file to opt out. Nothing else has to change."
  ],

  "mode": "ste-general",

  "_mode_comment": [
    "ste-general  READMEs, pull request text, docs, release notes. Structure",
    "             rules and the slop list. No dictionary lockdown, so ordinary",
    "             words stay.",
    "ste-strict   Procedures, runbooks, safety text, error messages. Adds the",
    "             approved-word allowlist: every word must be in the STE",
    "             dictionary or in the glossary below.",
    "",
    "'mode' is the default for the repository. Use 'strict_paths' below for the",
    "files that must be stricter than that default."
  ],

  "strict_paths": [],

  "_strict_paths_comment": [
    "Paths that get ste-strict whatever 'mode' says, as glob patterns. The mode",
    "belongs to the text, not to the repository: a runbook is a runbook even in",
    "a repository that is mostly prose. The hook reads this, so safety text is",
    "gated at strict without a person choosing the mode each time.",
    "",
    "For example:",
    "  [\"runbooks/**\", \"docs/procedures/**\", \"**/ERRORS.md\"]"
  ],

  "glossary": [
    "agent", "API", "branch", "cache", "CLI", "commit", "config", "dependency",
    "endpoint", "environment", "file", "flag", "hook", "JSON", "linter", "log",
    "merge", "path", "prose", "repository", "schema", "script", "server",
    "session", "skill", "token", "workflow"
  ],

  "_glossary_comment": [
    "Technical nouns, per rules 1.6 and 1.8. In ste-strict a word that is not in",
    "the STE dictionary fails unless it is listed here. Above is a starting set",
    "for software work. Add your own product and domain nouns.",
    "",
    "For a long list, use \"glossary_file\" instead and point it at a text file of",
    "one word per line, relative to this file."
  ],

  "one_name_for_one_thing": {},

  "_one_name_comment": [
    "Rule 1.11, one name for one thing. Each key is the name to use, and its",
    "list holds the names it replaces. Empty by default: a pair like",
    "  \"repository\": [\"repo\"]",
    "warns on every 'repo' in the project, which is only useful once you have",
    "decided that it matters. A word cannot be in both this list and the",
    "glossary above."
  ],

  "exclude": [
    "CHANGELOG.md",
    "LICENSE",
    "**/vendor/**",
    "**/node_modules/**"
  ]
}
